paths:
  - /tmp/suspicious-watch # does not exist
  # Einfacher Pfad (wie bisher):
  - /home/nils/.ssh
  # Pfad als Objekt mit Binary-Whitelist:
  - path: /home/nils/dotfiles/nvim
    allow_binaries:
      - /etc/profiles/per-user/nils/bin/nvim

allow_all: false
